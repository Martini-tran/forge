// Tailwind CSS v4 via PostCSS. The string-keyed plugin form lets postcss-load-config
// import the (ESM-only) plugin itself, so this works in a CommonJS project.
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
