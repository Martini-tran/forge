# 插件市场 / 后端接口的基地址 = nebula 网关（nebula-service-gateway）。
# 渲染进程的 requestClient 读取此变量（见 src/renderer/api/request.ts）；
# 接口路径自带 /forge 前缀（网关按 /forge/** 路由到 forge 服务）。
# 复制本文件为 .env 并填入实际地址；留空表示与页面同源 / 相对路径。
VITE_API_BASE_URL=http://localhost:19000
